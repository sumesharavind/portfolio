import React from 'react';
import './SendButton.css'; // Ensure this file contains the required CSS

const SendButton = () => {
  return (
    <button> Send
    </button>
);
};
export default SendButton;
